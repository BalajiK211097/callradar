# Note: spaCy is no longer needed — entity extraction is handled by Deepgram Nova-2 (detect_entities=True)

# Transcription API
deepgram-sdk>=3.0.0,<4.0.0

# LLM API
openai>=1.0.0

# Data models
pydantic>=2.0.0

# Environment
python-dotenv>=1.0.0

# AWS — Parameter Store secrets bootstrap + S3
boto3>=1.34.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
